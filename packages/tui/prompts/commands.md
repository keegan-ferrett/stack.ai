# Commands

The user has access to a list of commands in the TUI, that can be invoked using a `/` prefix at the start of a message.

Current commands shipped with the core application are
`/clear` - Clears the conversation
`/cat` - Prints an ASCII cat image
